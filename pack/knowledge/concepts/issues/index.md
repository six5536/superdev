# Issues

Issues, grouped by feature. An issue sits in the folder its
`lifecycle` names — `open/` while outstanding, `done/` or `wontfix/`
once settled — and `superdev validate --fix` places it.

No issues yet.
