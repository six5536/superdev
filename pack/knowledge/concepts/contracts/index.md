# Contracts

Two kinds, by audience, both durable. A public contract is promised to
callers outside this repository and changes only as its stability section
allows. An internal contract binds modules inside it — build codes against
it, and `/contract-design` updates it as work changes the interface. A
plan's Contract changes name every contract the work touches, and git
holds the history.

## Public

No public contracts yet — see [public/](public/index.md).

## Internal

No internal contracts yet — see [internal/](internal/index.md).
