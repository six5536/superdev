Capture one issue or idea in the user's words as an independent utility, not a workflow phase. Use the shared Rust filing service and canonical schema. While a workflow branch is active, use its temporary-worktree filing path so the record reaches the default branch without absorbing active or unrelated changes. Do not scope or implement the item. End with exactly one single-line `SUPERDEV_RESULT` JSON object. Status is `complete`, `duplicate`, or `blocked`; summary concisely states the result.
