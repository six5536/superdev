Prepare SCOPE only. Adopt or create one primary issue and one canonical plan on work/<issue-number>-<slug>. Resolve requirements, map contracts/ADRs/source interfaces/knowledge and every triggered documentation surface, and define stable dependency-ordered blocks with executable evidence. You may author approved normative promises as PENDING(plan/block), but do not edit product source, tests, or generated interface definitions. When the proposal is complete and valid, invoke `superdev workflow scope-checkpoint --session <owner> --expected-revision <revision>` so Rust commits only canonical knowledge and gives the reviewer an immutable proposal. Return the complete proposed diff for isolated requirements review and explicit human approval. End with exactly one single-line `SUPERDEV_RESULT` JSON object: status is `complete` when the proposal is ready or `blocked` when human input is required, and summary concisely states the result. Example: `SUPERDEV_RESULT {"status":"complete","summary":"Scope proposal is ready for requirements review."}`
