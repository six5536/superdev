---
type: Template
id: template-visual-system
title: Visual System Template
description: Knowledge concept skeleton — the design tokens later slices build against — palette, type roles, spacing, signature, and component library.
status: stable
---

---
type: Convention
id: visual-system
title: Visual System
description: The design tokens the UI is built against — <one line: the direction in a phrase>.
status: stable
---

<The aesthetic direction in one or two sentences: the point of view, and the one risk it takes. Every UI slice builds against these tokens; changing them is a decision, not a tweak.>

# Palette

- <token-name> `<#hex>` — <role: background, text, accent, …>
- <token-name> `<#hex>` — <4–6 named values in all>

# Type

- Display: <typeface> — <where it appears, and the restraint on it.>
- Body: <typeface> — <the complementary workhorse.>
- Utility: <typeface> — <captions or data, if needed.>

<The scale: sizes, weights, and spacing worth naming.>

# Layout & spacing

<The layout concept in a sentence, and the spacing scale or grid the components sit on.>

# Signature

<The single element this design is remembered by, and where it appears.>

# Component library

<The library or component set in use, and the rule for adding to it.>
